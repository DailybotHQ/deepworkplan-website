---
/**
 * OutputWorkspace (SP-04) — the gitignored `.dwp/` workspace root:
 * plans/PLAN_<name>/ with its analysis outputs. Spec:
 * docs/visuals/prompts/03-spec.md#sp-04. HTML/CSS file tree; paths mono (stay
 * English), annotations localized. role="img"; dark mode; zero JS.
 */
import type { Language } from '@/lib/i18n';

interface Props {
  lang?: Language;
  class?: string;
}

const { lang = 'en', class: className = '' } = Astro.props;

const i18n = {
  en: {
    ariaLabel:
      'The gitignored .dwp/ output workspace: a plans/ folder containing PLAN_name/ with README, PROGRESS, task files, and an analysis_results folder.',
    title: 'Output workspace',
    ann: {
      root: 'gitignored · disposable',
      results: 'reports',
      review: 'security review',
      report: 'executive report',
    },
    caption:
      'All plan output lives under a gitignored `.dwp/` workspace, kept separate from tracked source.',
  },
  es: {
    ariaLabel:
      'El espacio de trabajo de salida .dwp/ (ignorado por git): una carpeta plans/ que contiene PLAN_name/ con README, PROGRESS, archivos de tarea y una carpeta analysis_results.',
    title: 'Espacio de trabajo de salida',
    ann: {
      root: 'ignorado por git · desechable',
      results: 'informes',
      review: 'revisión de seguridad',
      report: 'informe ejecutivo',
    },
    caption:
      'Toda la salida de los planes reside en un espacio de trabajo `.dwp/` ignorado por git, separado del código versionado.',
  },
  pt: {
    ariaLabel:
      'O espaço de trabalho de saída .dwp/ (ignorado pelo git): uma pasta plans/ que contém PLAN_name/ com README, PROGRESS, arquivos de tarefa e uma pasta analysis_results.',
    title: 'Espaço de trabalho de saída',
    ann: {
      root: 'ignorado pelo git · descartável',
      results: 'relatórios',
      review: 'revisão de segurança',
      report: 'relatório executivo',
    },
    caption:
      'Toda a saída dos planos reside em um espaço de trabalho `.dwp/` ignorado pelo git, separado do código versionado.',
  },
  zh: {
    ariaLabel:
      '被 git 忽略的 .dwp/ 输出工作区：包含一个 plans/ 文件夹，其中有 PLAN_name/，内含 README、PROGRESS、任务文件以及一个 analysis_results 文件夹。',
    title: '输出工作区',
    ann: {
      root: '被 git 忽略 · 可丢弃',
      results: '报告',
      review: '安全审查',
      report: '执行摘要报告',
    },
    caption:
      '所有计划的输出都存放在被 git 忽略的 `.dwp/` 工作区中，与受版本控制的源代码分开保存。',
  },
  ja: {
    ariaLabel:
      'git で無視される .dwp/ 出力ワークスペース：README、PROGRESS、タスクファイル、analysis_results フォルダを含む PLAN_name/ を内包する plans/ フォルダで構成されます。',
    title: '出力ワークスペース',
    ann: {
      root: 'git 無視 ・ 破棄可',
      results: 'レポート',
      review: 'セキュリティレビュー',
      report: 'エグゼクティブレポート',
    },
    caption:
      'プランのすべての出力は、git で無視される `.dwp/` ワークスペースに置かれ、追跡対象のソースとは別に管理されます。',
  },
  de: {
    ariaLabel:
      'Der von git ignorierte Ausgabe-Arbeitsbereich .dwp/: ein Ordner plans/ mit PLAN_name/, der README, PROGRESS, Aufgabendateien und einen Ordner analysis_results enthält.',
    title: 'Ausgabe-Arbeitsbereich',
    ann: {
      root: 'von git ignoriert · verwerfbar',
      results: 'Berichte',
      review: 'Sicherheitsüberprüfung',
      report: 'Management-Bericht',
    },
    caption:
      'Die gesamte Plan-Ausgabe liegt in einem von git ignorierten `.dwp/`-Arbeitsbereich, getrennt vom versionierten Quellcode.',
  },
  fr: {
    ariaLabel:
      'L’espace de travail de sortie .dwp/ ignoré par git : un dossier plans/ contenant PLAN_name/ avec README, PROGRESS, des fichiers de tâche et un dossier analysis_results.',
    title: 'Espace de travail de sortie',
    ann: {
      root: 'ignoré par git · jetable',
      results: 'rapports',
      review: 'revue de sécurité',
      report: 'rapport exécutif',
    },
    caption:
      'Toute la sortie des plans réside dans un espace de travail `.dwp/` ignoré par git, séparé du code versionné.',
  },
  ko: {
    ariaLabel:
      'git이 무시하는 .dwp/ 출력 작업 공간: README, PROGRESS, 작업 파일, analysis_results 폴더가 들어 있는 PLAN_name/을 포함하는 plans/ 폴더로 구성됩니다.',
    title: '출력 작업 공간',
    ann: {
      root: 'git 무시 · 폐기 가능',
      results: '보고서',
      review: '보안 검토',
      report: '경영 요약 보고서',
    },
    caption:
      '모든 플랜 출력은 git이 무시하는 `.dwp/` 작업 공간 안에 있어요 — 버전 관리되는 소스와 분리해서 보관해요.',
  },
  ru: {
    ariaLabel:
      'Игнорируемое git рабочее пространство вывода .dwp/: папка plans/, содержащая PLAN_name/ с README, PROGRESS, файлами задач и папкой analysis_results.',
    title: 'Рабочее пространство вывода',
    ann: {
      root: 'игнорируется git · одноразовое',
      results: 'отчёты',
      review: 'обзор безопасности',
      report: 'итоговый отчёт',
    },
    caption:
      'Весь вывод планов хранится в игнорируемом git рабочем пространстве `.dwp/`, отдельно от версионируемого исходного кода.',
  },
  it: {
    ariaLabel:
      'Lo spazio di lavoro di output .dwp/ ignorato da git: una cartella plans/ contenente PLAN_name/ con README, PROGRESS, file di attività e una cartella analysis_results.',
    title: 'Spazio di lavoro di output',
    ann: {
      root: 'ignorato da git · usa e getta',
      results: 'report',
      review: 'revisione di sicurezza',
      report: 'report esecutivo',
    },
    caption:
      'Tutto l’output dei piani risiede in uno spazio di lavoro `.dwp/` ignorato da git, separato dal codice versionato.',
  },
  tr: {
    ariaLabel:
      'git tarafından yok sayılan .dwp/ çıktı çalışma alanı: README, PROGRESS, görev dosyaları ve bir analysis_results klasörü içeren PLAN_name/ klasörünü barındıran bir plans/ klasörü.',
    title: 'Çıktı çalışma alanı',
    ann: {
      root: 'git tarafından yok sayılır · atılabilir',
      results: 'raporlar',
      review: 'güvenlik incelemesi',
      report: 'yönetici raporu',
    },
    caption:
      'Tüm plan çıktıları, git tarafından yok sayılan bir `.dwp/` çalışma alanında bulunur ve sürümlenen kaynaktan ayrı tutulur.',
  },
  id: {
    ariaLabel:
      'Ruang kerja keluaran .dwp/ yang diabaikan git: sebuah folder plans/ yang berisi PLAN_name/ dengan README, PROGRESS, berkas tugas, dan sebuah folder analysis_results.',
    title: 'Ruang kerja keluaran',
    ann: {
      root: 'diabaikan git · sekali pakai',
      results: 'laporan',
      review: 'tinjauan keamanan',
      report: 'laporan eksekutif',
    },
    caption:
      'Seluruh keluaran rencana berada di dalam ruang kerja `.dwp/` yang diabaikan git, terpisah dari kode sumber yang dilacak.',
  },
  vi: {
    ariaLabel:
      'Không gian làm việc đầu ra .dwp/ bị git bỏ qua: một thư mục plans/ chứa PLAN_name/ với README, PROGRESS, các tệp tác vụ và một thư mục analysis_results.',
    title: 'Không gian làm việc đầu ra',
    ann: {
      root: 'bị git bỏ qua · có thể loại bỏ',
      results: 'báo cáo',
      review: 'đánh giá bảo mật',
      report: 'báo cáo điều hành',
    },
    caption:
      'Toàn bộ đầu ra của kế hoạch nằm trong một không gian làm việc `.dwp/` bị git bỏ qua, tách khỏi mã nguồn được theo dõi.',
  },
  hi: {
    ariaLabel:
      'git द्वारा अनदेखा किया गया .dwp/ आउटपुट कार्यक्षेत्र: एक plans/ फ़ोल्डर जिसमें PLAN_name/ है, और उसके भीतर README, PROGRESS, कार्य फ़ाइलें तथा एक analysis_results फ़ोल्डर मौजूद है।',
    title: 'आउटपुट कार्यक्षेत्र',
    ann: {
      root: 'git द्वारा अनदेखा · हटाने योग्य',
      results: 'रिपोर्ट',
      review: 'सुरक्षा समीक्षा',
      report: 'कार्यकारी रिपोर्ट',
    },
    caption:
      'योजना का सारा आउटपुट git द्वारा अनदेखे किए गए `.dwp/` कार्यक्षेत्र में रहता है — ट्रैक किए गए स्रोत से अलग रखा जाता है।',
  },
  pl: {
    ariaLabel:
      'Ignorowana przez git przestrzeń robocza wyjścia .dwp/: folder plans/ zawierający PLAN_name/ z plikami README, PROGRESS, plikami zadań i folderem analysis_results.',
    title: 'Przestrzeń robocza wyjścia',
    ann: {
      root: 'ignorowane przez git · jednorazowe',
      results: 'raporty',
      review: 'przegląd bezpieczeństwa',
      report: 'raport wykonawczy',
    },
    caption:
      'Całe wyjście planów znajduje się w ignorowanej przez git przestrzeni roboczej `.dwp/`, oddzielone od wersjonowanego kodu źródłowego.',
  },
  uk: {
    ariaLabel:
      'Ігнорований git робочий простір виводу .dwp/: тека plans/, що містить PLAN_name/ із README, PROGRESS, файлами завдань та текою analysis_results.',
    title: 'Робочий простір виводу',
    ann: {
      root: 'ігнорується git · одноразове',
      results: 'звіти',
      review: 'огляд безпеки',
      report: 'підсумковий звіт',
    },
    caption:
      'Увесь вивід планів зберігається в ігнорованому git робочому просторі `.dwp/`, окремо від версіонованого вихідного коду.',
  },
  th: {
    ariaLabel:
      'พื้นที่ทำงานเอาต์พุต .dwp/ ที่ git ละเว้น ประกอบด้วยโฟลเดอร์ plans/ ซึ่งบรรจุ PLAN_name/ พร้อม README, PROGRESS, ไฟล์งาน และโฟลเดอร์ analysis_results',
    title: 'พื้นที่ทำงานเอาต์พุต',
    ann: {
      root: 'git ละเว้น · ทิ้งได้',
      results: 'รายงาน',
      review: 'การตรวจสอบความปลอดภัย',
      report: 'รายงานผู้บริหาร',
    },
    caption:
      'เอาต์พุตของแผนทั้งหมดอยู่ในพื้นที่ทำงาน `.dwp/` ที่ git ละเว้น โดยแยกออกจากซอร์สโค้ดที่อยู่ใต้การควบคุมเวอร์ชัน',
  },
} as const;

const t = i18n[lang as keyof typeof i18n] ?? i18n.en;

const rows = [
  { depth: 0, path: '.dwp/', ann: t.ann.root, accent: true },
  { depth: 1, path: 'plans/', ann: '' },
  { depth: 2, path: 'PLAN_<name>/', ann: '' },
  { depth: 3, path: 'README.md', ann: '' },
  { depth: 3, path: 'PROGRESS.md', ann: '' },
  { depth: 3, path: '<n>.task_<slug>.md', ann: '' },
  { depth: 3, path: 'analysis_results/', ann: t.ann.results },
  { depth: 4, path: 'SECURITY_REVIEW.md', ann: t.ann.review },
  { depth: 4, path: 'EXECUTIVE_REPORT.md', ann: t.ann.report },
];
---

<figure class={`editorial-asset workspace ${className}`} role="img" aria-label={t.ariaLabel}>
  <div class="editorial-asset__grid" aria-hidden="true"></div>
  <div class="editorial-asset__body">
    <span class="ea-label workspace-title">{t.title}</span>
    <ul class="workspace-list">
      {
        rows.map((r) => (
          <li
            class={`workspace-row${r.accent ? ' workspace-row--accent' : ''}`}
            style={`--depth:${r.depth}`}
          >
            <code class="workspace-path">{r.path}</code>
            {r.ann && <span class="workspace-ann">{r.ann}</span>}
          </li>
        ))
      }
    </ul>
  </div>
  <figcaption class="fig-caption">{t.caption}</figcaption>
</figure>

<style>
  .workspace {
    color: var(--color-ink);
  }
  .workspace-title {
    display: block;
    font-size: 0.8rem;
    margin-bottom: 0.75rem;
  }
  .workspace-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .workspace-row {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0.5rem 0.75rem;
    padding: 0.3rem 0;
    padding-left: calc(var(--depth) * 1.4rem);
    border-left: 1px solid var(--color-line);
  }
  .workspace-row--accent {
    border-left-color: var(--color-secondary);
  }
  .workspace-path {
    font-family: var(--font-mono);
    font-size: 0.88rem;
  }
  .workspace-row--accent .workspace-path {
    color: var(--color-secondary);
    font-weight: 600;
  }
  .workspace-ann {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.8rem;
    color: var(--color-muted);
  }
</style>
