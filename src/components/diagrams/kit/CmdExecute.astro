---
/**
 * CmdExecute (KT-05) — /dwp-execute as input → process → output.
 * Spec: docs/visuals/prompts/04-kit.md#kt-05. Composes CommandSequence.
 */
import type { Language } from '@/lib/i18n';
import CommandSequence from './CommandSequence.astro';

interface Props {
  lang?: Language;
  class?: string;
}
const { lang = 'en', class: className = '' } = Astro.props;

const i18n = {
  en: {
    ariaLabel:
      'dwp-execute: a plan runs task by task through validate-and-commit, producing completed, committed work.',
    input: 'Plan',
    process: 'Task → validate → commit',
    output: 'Completed, committed work',
    caption:
      '/dwp-execute runs the plan task by task, validating and committing each.',
  },
  es: {
    ariaLabel:
      'dwp-execute: un plan se ejecuta tarea por tarea mediante validar-y-confirmar, produciendo trabajo completado y confirmado.',
    input: 'Plan',
    process: 'Tarea → validar → commit',
    output: 'Trabajo completado y confirmado',
    caption:
      '/dwp-execute ejecuta el plan tarea por tarea, validando y confirmando cada una.',
  },
  pt: {
    ariaLabel:
      'dwp-execute: um plano é executado tarefa por tarefa por meio de validar-e-fazer-commit, produzindo trabalho concluído e versionado.',
    input: 'Plano',
    process: 'Tarefa → validar → commit',
    output: 'Trabalho concluído e versionado',
    caption:
      '/dwp-execute executa o plano tarefa por tarefa, validando e versionando cada uma.',
  },
  zh: {
    ariaLabel:
      'dwp-execute：计划逐个任务运行，经过验证与提交流程，最终产出已完成并已提交的工作成果。',
    input: '计划',
    process: '任务 → 验证 → 提交',
    output: '已完成并已提交的工作',
    caption: '/dwp-execute 按任务逐一执行计划，对每个任务进行验证与提交。',
  },
  ja: {
    ariaLabel:
      'dwp-execute: 計画はタスクごとに「検証してコミットする」処理を経て実行され、完了済みかつコミット済みの成果物が得られます。',
    input: '計画',
    process: 'タスク → 検証 → コミット',
    output: '完了してコミットされた成果物',
    caption:
      '/dwp-execute は計画をタスクごとに実行し、それぞれを検証してコミットします。',
  },
  de: {
    ariaLabel:
      'dwp-execute: Ein Plan wird Aufgabe für Aufgabe über Validieren und Committen ausgeführt und liefert abgeschlossene, committete Arbeit.',
    input: 'Plan',
    process: 'Aufgabe → validieren → committen',
    output: 'Abgeschlossene, committete Arbeit',
    caption:
      '/dwp-execute führt den Plan Aufgabe für Aufgabe aus und validiert und committet jede einzelne.',
  },
  fr: {
    ariaLabel:
      'dwp-execute : un plan s’exécute tâche par tâche via valider-et-committer, produisant un travail terminé et committé.',
    input: 'Plan',
    process: 'Tâche → valider → commit',
    output: 'Travail terminé et committé',
    caption:
      '/dwp-execute exécute le plan tâche par tâche, en validant et en committant chacune.',
  },
  ko: {
    ariaLabel:
      'dwp-execute: 계획이 작업 단위로 검증과 커밋 과정을 거쳐 실행되고, 완료되어 커밋된 작업물이 산출돼요.',
    input: '계획',
    process: '작업 → 검증 → 커밋',
    output: '완료되어 커밋된 작업물',
    caption:
      '/dwp-execute는 계획을 작업 단위로 실행하면서 각 작업을 검증하고 커밋해요.',
  },
  ru: {
    ariaLabel:
      'dwp-execute: план выполняется задача за задачей через цикл «проверить и закоммитить», давая на выходе завершённую и зафиксированную работу.',
    input: 'План',
    process: 'Задача → проверка → коммит',
    output: 'Завершённая и зафиксированная работа',
    caption:
      '/dwp-execute выполняет план задача за задачей, проверяя и фиксируя каждую.',
  },
  it: {
    ariaLabel:
      'dwp-execute: un piano viene eseguito un compito alla volta tramite valida-e-committa, producendo lavoro completato e committato.',
    input: 'Piano',
    process: 'Compito → validare → commit',
    output: 'Lavoro completato e committato',
    caption:
      '/dwp-execute esegue il piano un compito alla volta, validando e committando ciascuno.',
  },
  tr: {
    ariaLabel:
      'dwp-execute: bir plan, doğrula-ve-commit’le adım adım, görev görev çalışır ve tamamlanmış, commit edilmiş iş üretir.',
    input: 'Plan',
    process: 'Görev → doğrula → commit',
    output: 'Tamamlanmış, commit edilmiş iş',
    caption:
      '/dwp-execute planı görev görev çalıştırır; her birini doğrular ve commit eder.',
  },
  id: {
    ariaLabel:
      'dwp-execute: sebuah rencana dijalankan tugas demi tugas melalui validasi-dan-commit, menghasilkan pekerjaan yang selesai dan sudah di-commit.',
    input: 'Rencana',
    process: 'Tugas → validasi → commit',
    output: 'Pekerjaan selesai dan ter-commit',
    caption:
      '/dwp-execute menjalankan rencana tugas demi tugas, memvalidasi dan meng-commit setiap tugas.',
  },
  vi: {
    ariaLabel:
      'dwp-execute: một kế hoạch được chạy theo từng tác vụ qua quy trình xác minh-và-commit, tạo ra phần việc đã hoàn thành và đã commit.',
    input: 'Kế hoạch',
    process: 'Tác vụ → xác minh → commit',
    output: 'Phần việc đã hoàn thành và đã commit',
    caption:
      '/dwp-execute chạy kế hoạch theo từng tác vụ, xác minh và commit từng tác vụ một.',
  },
  hi: {
    ariaLabel:
      'dwp-execute: एक योजना कार्य-दर-कार्य सत्यापित-करो-और-कमिट-करो प्रक्रिया से चलती है और पूर्ण, कमिट किया गया काम तैयार करती है।',
    input: 'योजना',
    process: 'कार्य → सत्यापन → कमिट',
    output: 'पूर्ण, कमिट किया गया काम',
    caption:
      '/dwp-execute योजना को कार्य-दर-कार्य चलाता है, प्रत्येक को सत्यापित और कमिट करते हुए।',
  },
  pl: {
    ariaLabel:
      'dwp-execute: plan jest wykonywany zadanie po zadaniu w cyklu zweryfikuj-i-zacommituj, dając ukończoną i zacommitowaną pracę.',
    input: 'Plan',
    process: 'Zadanie → weryfikacja → commit',
    output: 'Ukończona i zacommitowana praca',
    caption:
      '/dwp-execute wykonuje plan zadanie po zadaniu, weryfikując i commitując każde z nich.',
  },
  uk: {
    ariaLabel:
      'dwp-execute: план виконується задача за задачею через цикл «перевірити та закомітити», даючи на виході завершену й зафіксовану роботу.',
    input: 'План',
    process: 'Задача → перевірка → коміт',
    output: 'Завершена й зафіксована робота',
    caption:
      '/dwp-execute виконує план задача за задачею, перевіряючи й фіксуючи кожну з них.',
  },
  th: {
    ariaLabel:
      'dwp-execute: แผนถูกดำเนินการทีละงานผ่านขั้นตอนตรวจสอบและคอมมิต โดยให้ผลลัพธ์เป็นงานที่เสร็จสมบูรณ์และคอมมิตแล้ว',
    input: 'แผน',
    process: 'งาน → ตรวจสอบ → คอมมิต',
    output: 'งานที่เสร็จสมบูรณ์และคอมมิตแล้ว',
    caption: '/dwp-execute รันแผนทีละงาน โดยตรวจสอบและคอมมิตในแต่ละงาน',
  },
} as const;
const t = i18n[lang as keyof typeof i18n] ?? i18n.en;
---

<CommandSequence
  cmd="/dwp-execute"
  input={t.input}
  process={t.process}
  output={t.output}
  ariaLabel={t.ariaLabel}
  caption={t.caption}
  class={className}
/>
