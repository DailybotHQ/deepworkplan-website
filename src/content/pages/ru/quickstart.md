---
title: "Быстрый старт — Deep Work Plan"
description: "Установите навык, выполните онбординг репозитория, затем планируйте и выполняйте работу с любым агентом — шаги, делающие репозиторий spec-driven и пилотируемым."
lastUpdated: 2026-05-31
---

## Быстрый старт

Установите навык, выполните онбординг репозитория, затем планируйте и выполняйте работу с любым агентом — шаги, которые делают
ваш репозиторий spec-driven и пилотируемым агентами.

## Путь внедрения

### 1. Установите навык Deep Work Plan

Добавьте навык в свой репозиторий. Он поставляется с маршрутизатором плюс восемью под-навыками — create, execute, refine,
resume, status, verify, onboard и author. Используйте Skills CLI для самого быстрого пути:

```bash
npx skills add DailybotHQ/deepworkplan-skill
```

Или клонируйте репозиторий и запустите setup там, где доступны git и оболочка:

```bash
git clone https://github.com/DailybotHQ/deepworkplan-skill.git && cd deepworkplan-skill && ./setup.sh
```

### 2. Выполните онбординг репозитория

Запустите под-навык onboard и позвольте агенту рассудить о вашем реальном репозитории — его стеке, менеджере
пакетов и реальных командах валидации:

```bash
/deepworkplan-onboard
```

Он создаёт `AGENTS.md`, базу знаний `docs/`, документацию по модулям и межагентный дом `.agents/`
(с символьной ссылкой `.claude → .agents`), подключает тонкие команды `dwp-*` и разворачивает игнорируемую git-ом
`.dwp/` для планов и черновиков. Ничего не берётся из шаблона; всё адаптируется под ваш репозиторий.

### 3. Развивайте набор и принимайте дополнения

Используйте `/skill-create` и `/agent-create` (под-навык author), чтобы растить подходящие стеку навыки,
агентов и команды. Онбординг также предлагает три опциональных дополнения — devcontainer, Dailybot и
dependency-upgrade, — которые вы принимаете, только когда они уместны. Репозиторий полностью соответствует стандарту и с нулём дополнений.

### 4. Планируйте и выполняйте

Создайте Deep Work Plan и выполняйте его задача за задачей:

```bash
/dwp-create <goal>
/dwp-execute
```

Используйте `/dwp-status`, `/dwp-refine` и `/dwp-resume` по ходу работы. Каждый план несёт пронумерованные задачи,
validation gates и протокол завершения, чтобы работа оставалась структурированной и возобновляемой между сессиями.

## Результат

Ваш репозиторий становится spec-driven и пилотируемым агентами: план — это долговечный источник истины, а
сам репозиторий становится harness, по которому работает любой агент.

- [Читать методологию](/methodology)
- [Просмотреть спецификацию](/spec)
- [Изучить набор](/kit)
