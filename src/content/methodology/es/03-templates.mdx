---
title: Plantillas de plan y tarea
description: "La estructura de los archivos de plan y de tarea, el Final Review con el que todo plan debe terminar y las convenciones de README y PROGRESS que mantienen legible el plan."
order: 3
lang: es
summary: Cómo se estructuran los planes y las tareas en disco, y las convenciones que los mantienen legibles.
icon: document
---
import PlanTree from '@/components/diagrams/methodology/PlanTree.astro';

# Plantillas de plan y tarea

Un Deep Work Plan es un directorio de archivos Markdown. La forma de ese directorio, y de cada archivo que contiene, es lo que hace que un plan sea revisable tanto por personas como por agentes.

## Estructura del plan

<PlanTree lang="es" class="not-prose my-10" />

Un plan es un directorio bajo `.dwp/plans/` llamado `PLAN_<slug>/`. Contiene:

- `README.md`: el resumen del plan: objetivo, material de origen, tabla de tareas y estado.
- Un archivo por tarea, con el nombre `<n>.task_<slug>.md`.
- `PROGRESS.md`: un registro continuo de la ejecución.

## Estructura del archivo de tarea

Cada archivo de tarea se llama `<n>.task_<slug>.md` y sigue la anatomía de diez secciones: Objetivo, Contexto, Superficie tocada, Pasos, Criterios de aceptación, Validación, Archivos, Dependencias, Riesgos y Finalización y registro. Las secciones aparecen siempre en ese orden para que cualquier lector sepa dónde mirar. Cada tarea se reancla al objetivo del plan antes de actuar, lo que evita que un agente se desvíe en un horizonte largo de varias horas.

## El Final Review

Todo plan debe terminar con exactamente una tarea obligatoria: el **Final Review**. Cierra el plan en tres pases ordenados — nada del pase de seguridad se relaja:

1. **Pase de seguridad**: auditar todo lo que el plan cambió en busca de secretos, riesgos de inyección y nueva superficie de ataque, y mantener `docs/SECURITY.md` al día. La seguridad no es un flujo de trabajo aparte añadido al final: cada plan deja sus propios cambios auditados, y un hallazgo crítico bloquea la finalización.
2. **Validación de estado final**: la validación completa del repositorio se ejecuta y pasa sobre el último estado relevante, tras la corrección final.
3. **Reconciliación de skills**: cada tarea registró su propia disposición de skills mientras su evidencia estaba fresca; la revisión reconcilia el registro. El Reporte Ejecutivo sigue disponible a petición.

## Convenciones de README y PROGRESS

El `README.md` del plan debe contener un título (`# Deep Work Plan: <nombre>`), una declaración del objetivo en prosa, una sección opcional de material de origen, una tabla de tareas (número, nombre de la tarea, casilla de estado) y una línea de estado del plan con la forma `<n>/<total> tareas completadas`.

`PROGRESS.md` es un registro de ejecución de solo adición. Cada entrada registra una marca de tiempo en formato ISO 8601, el número y nombre de la tarea, qué se hizo y cualquier desviación o motivo de omisión. Como solo crece, es un historial fiable de cómo se desarrolló realmente el plan.

## Encabezados y referencias cruzadas

Todos los encabezados usan mayúscula inicial al estilo de oración, y los documentos evitan el lenguaje publicitario y los signos de exclamación. Estas convenciones mantienen los planes coherentes entre agentes y a lo largo del tiempo.
