---
title: 代码仓库原型
description: "单一代码仓库与编排枢纽两种原型、对仓库进行归类的判定法则，以及接入与计划范围在两者之间的差异。"
order: 5
lang: zh
summary: 为何代码仓库的原型决定了代理如何接入并为计划界定范围。
icon: layers
---
import ArchetypesTree from '@/components/diagrams/methodology/ArchetypesTree.astro';

# 代码仓库原型

DWP 识别两种代码仓库原型。代理所推断出的原型，决定了它如何接入，以及如何为计划界定范围，因此正确识别它是任何一次协作中的第一项决策。

## 单一代码仓库

一个自成一体的代码库——一个应用、一个库或一个服务。计划直接作用于仓库中的代码，接入则读取仓库自身的结构与约定。

特征：

- 单一、连贯的代码库。
- 计划修改本仓库中的文件。
- `.dwp/` 工作区位于仓库根目录。

## 编排枢纽

一个管理多个子仓库的协调型仓库。计划可以在子仓库中派生出子计划，接入则读取枢纽中所管理仓库的登记表，而非单一代码库。

特征：

- 协调多个子仓库。
- 计划可以委派给子计划。
- 维护一份所管理仓库的登记表。
- 位于枢纽根目录的 `.dwp/` 工作区追踪跨仓库的状态。

## 归类判定法则

<ArchetypesTree lang="en" class="not-prose my-10" />

当代理发现多个嵌套的 git 仓库或子模块、一份所管理仓库的登记表或清单，或指向外部仓库的配置时，就应当把该仓库归类为编排枢纽。在缺乏这些信号的情况下，它会把该仓库当作单一代码仓库来对待。

## 接入有何不同

| 方面 | 单一 | 编排 |
|--------|------------|--------------|
| 范围 | 本仓库 | 多个仓库 |
| 接入 | 仓库结构 | 枢纽登记表 |
| 计划目标 | 本地文件 | 子计划 |
| 状态 | 本地 `.dwp/` | 跨仓库 `.dwp/` |

其实际效果是：单一仓库代理会对一个代码库进行端到端的推理，而编排代理则对协调本身进行推理——哪个子仓库负责哪部分工作，以及跨仓库的状态如何保持一致。

把原型判断对，正是让代理得以在无人监督下自主工作数小时的关键：它把计划、接入与状态界定到正确的边界上，于是代理从第一项任务到最后一项都在正确的对象面上运作。
